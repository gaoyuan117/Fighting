<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:id="@+id/invite_add_group_rela"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:descendantFocusability="blocksDescendants">

    <CheckBox
        android:id="@+id/invite_add_group_checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="20dp"
        android:background="@drawable/bg_checkbox_cricle"
        android:button="@null" />


       <!-- <com.facebook.drawee.view.SimpleDraweeView
            android:id="@+id/friend_item_photo"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginBottom="8dp"
            android:layout_marginLeft="12dp"
            android:layout_toRightOf="@+id/invite_add_group_checkbox"
            android:layout_marginTop="8dp"
            fresco:failureImage="@drawable/ic_photo"
            fresco:roundedCornerRadius="1000dp"
            fresco:placeholderImage="@drawable/ic_photo"
            />-->
    <com.facebook.drawee.view.SimpleDraweeView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:id="@+id/item_avatar"
        android:layout_centerVertical="true"
        android:layout_marginLeft="15dp"
        android:layout_toRightOf="@+id/invite_add_group_checkbox"
        fresco:placeholderImage="@drawable/ic_logo"
        fresco:actualImageScaleType="centerCrop"
        fresco:roundAsCircle="true"
        />
        <TextView
            android:id="@+id/friend_item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@id/item_avatar"
            android:text="昵称"
            android:textColor="@color/text_nomal"
            android:textSize="14sp" />

</RelativeLayout>